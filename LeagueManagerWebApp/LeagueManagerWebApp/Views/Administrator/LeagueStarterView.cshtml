@model List<LeagueModel>
@{
    ViewData["Title"] = "LeagueStarterView";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LeagueStarterView</h2>

<div class="container-fluid">
    <div class="row justify-content-center">
        <form asp-controller="Administrator" asp-action="StartLeague" method="post">
            <div class="container-fluid">
                <div class="row justify-content-center">
                    <div class="col">
                        League name: <br />
                    </div>
                    <div class="col">
                        <select name="Name">
                            @foreach (var elem in Model)
                            {
                                <option value="@(elem.Name)">@(elem.Name)</option>
                            }
                        </select>
                    </div>
                </div>
                <div class="row justify-content-center">
                    <div class="col">
                        <input type="submit" />
                    </div>

                </div>
            </div>
        </form>
    </div>
</div>